name: "UAE PDF to OFX"
version: "1.0.0"
slug: "uae_pdf_to_ofx"
description: "Converts UAE bank statement PDFs to OFX format via HTTP API"
arch:
  - aarch64
  - amd64
startup: application
boot: auto
ports:
  8099/tcp: 8099
ports_description:
  8099/tcp: "FastAPI HTTP service"
map:
  - data
options:
  port: 8099
schema:
  port: int
