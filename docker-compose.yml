services:
  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    ports:
      - "8001:8000"
    volumes:
      - ofx_data:/data/ofx
    env_file:
      - .env
    restart: unless-stopped

volumes:
  ofx_data:
